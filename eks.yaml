ingress:
  enabled: true
  className: alb
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
  hosts:
    - host: simple-upload.eks
      paths:
        - path: /*
          pathType: ImplementationSpecific